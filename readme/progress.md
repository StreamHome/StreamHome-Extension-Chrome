# Progress Status

## What Works
- [x] **API Ingestion & Documentation:** External sources can successfully POST new media tasks to the server. Complete structure and samples documented in `@memory-bank\mediaSenderAPI.md`.
- [x] **Clean Ingestion Request Models:** Formulated movie payloads to fully exclude `"season"` and `"episode"` parameters in order to simplify integrations, updating `Dashboard.tsx` form, `test_ingest.py`, and `mediaSenderAPI.md`.
- [x] **Buttery-Smooth Seek Debouncing:** Solved rapid-seek browser decoder lag and audio squeaking by accumulating forward/backward commands and scrubbing ticks, updating the UI slider instantly, and debouncing actual video-element seek operations by 250ms with a temporal event guard.
- [x] **Dynamic Ingestion Preservations and Player Controls:** Pinpointed database paths absolutely to `server/database.db` to prevent duplicate DB instantiations, updated the ingestion worker to save dynamic qualities, subtitles on disk, and audio language details to local `.metadata/metadata.json` configs, and re-engineered `VideoPlayer.tsx` to dynamically parse and render quality options, map specific subtitle formats, and display active audio languages (restricting to exactly sended options with no dummy values).
- [x] **Background Queue:** Downloads are processed asynchronously.
- [x] **TMDB Integration:** Movie and TV show metadata is automatically fetched and mapped.
- [x] **Media Merging:** FFmpeg successfully merges video and audio streams when required.
- [x] **Asset Colocation:** Images are cached securely within their respective media directories.
- [x] **Database Resiliency & Upgrades:** The startup `sync_media_from_disk` hook successfully recovers deleted SQLite databases using local `.metadata` registries and upgrades placeholder assets automatically when credentials are set.
- [x] **Language Customization:** Audio language explicitly passed from the sender is accurately persisted in the database.
- [x] **Dynamic Rename Refactoring:** Added automatic directory and filename renaming when cataloging media, instantly converting placeholder directories (e.g. `Captured Movie...`) to TMDB titles (e.g. `Fight Club_1999_TMDB_550`).
- [x] **Environment Path Bug Fix:** Standardized `.env` loading using path-independent resolving relative to `config.py`, ensuring token credentials load correctly under uvicorn.
- [x] **Responsive Card Scaling:** Updated frontend rows to dynamically fit exactly 5 media cards on desktop viewports, scaling down responsively on smaller screens.
- [x] **Overlay Gutter Scroll Alignment:** Shifted margins to align scroll buttons inside screen margins (gutters) so they do not overlap card contents.
- [x] **Unified Details Modal Actions:** Configured all cards (downloaded or discover) to open the movie details modal first. Discover modals render a "Download / Ingest" button that triggers the pre-filled downloads form.
- [x] **Watchlist & Search Grid Card Sizing:** Optimized Watchlist, Search results, and skeleton loading grids to display maximum 5 columns on desktop, increasing baseline card scale by 20% to match the homepage catalog format.
- [x] **Card Hover Content Fitment:** Compacted the hover card elements (reducing padding, margins, button dimensions, and description clamp to 1 line) to guarantee all content fits inside 16:9 boxes without clipping on any column density.
- [x] **Background Banner Spaces Resolution:** Enclosed inline background image parameters inside double quotes and applied `encodeURI()` across the home banner layout and detail view layouts. This solves load failures caused by unquoted spaces in paths (e.g. `Fight Club`).
- [x] **Hero Banner Footprint & Spacing Scaling:** Constrained the hero banner container height (`h-[56.25vw] md:h-[70vh] min-h-[420px] max-h-[720px]`) and integrated a matching bottom offset (`bottom-14`) and negative row margin (`-mt-12`). This locks the catalog rows directly below the action buttons on all screen sizes, eliminating empty space gaps.
- [x] **Dynamic Discover Media Type Filtering:** Restructured discover APIs and frontend calls to filter recommendations by tab (Home vs Movies vs Series). Under Series view, it fetches TV shows (using TMDB `/discover/tv` and TV genre IDs `10759`/`10765`), while movie views query movies, preventing type overlaps.
- [x] **TV Series Local Caching & Nested Episodes:** Fixed series recommendations by aligning the discover folder name predictor with the actual directory structure (`{clean_title}_TMDB_{tmdb_id}`). Fully implemented nested seasons and episodes caching inside both the download queue manager and recommendation system, automatically generating `.metadata` folders, `metadata.json` files, and local images (`poster.jpg`, `backdrop.jpg`, `thumbnail.jpg`) inside each episode folder. Solved Windows-specific file descriptor crashes (`ValueError: too many file descriptors in select()`) by implementing a global image download semaphore, and upgraded logs to show exactly which season/episode is caching. Re-catalog loop (`sync_media_from_disk`) is upgraded to seamlessly parse both styles of directories, strictly verifying physical media presence to prevent placeholder restoration.
- [x] **Dynamic Web Seasons & Episodes Fetching:** Created a backend endpoint (`/api/series/{tmdb_id}/episodes`) and integrated a frontend `useEffect` hook to fetch and map exact seasons, episode numbers, descriptions, and correct durations (lengths) dynamically for all shows, resolving mock placeholders across all detail pages. Solved single-episode database limitations by introducing a hybrid TMDB + SQLite lookup model that displays all available seasons/episodes while enriching downloaded ones with local `video_url` play routes.
- [x] **Active Profile Settings Decoupling:** Implemented form draft buffer properties keeping variable style updates locked in form memory until database-side saves occur, solving dynamic checkbox/color preview shift issues.
- [x] **Tab Navigation Details Dismissal:** Updated tab-selection handlers to clear details modal states and movie query params automatically, preventing Details page navigation blocks.
- [x] **Netflix Theme Polish & Animations:** Ingested Bebas Neue/Montserrat fonts, created scale hover cards showing Montserrat badges and red borders, and elevated details modals with a crimson border glow (`shadow-[0_0_50px_rgba(229,9,20,0.15)]`), a taller banner image container, and blocky buttons.
- [x] **Layout Zoom Clipping & Separator Spacing:** Resolved Ken Burns scaling billboard backdrop overflows by adding `overflow-hidden` to `#featured-banner`, and aligned the billboard action buttons cleanly from catalog lists via bottom offset/top margin adjustments.
- [x] **Global Structured Logging:** Standardized standard python logging using `RotatingFileHandler` writing logs to `server/temp/app.log` (5MB max size, 3 backup files) and console stream, replacing all raw `print()` statements in route/service logic.
- [x] **Database Schema Auto-Migrations:** Added `error_message`, `has_video`, `has_audio`, and `scan_quality` fields to `DownloadTask`, with dynamic startup auto-migrations in `db.py`.
- [x] **Media Ingestion & Stream Scanner:** Enforced 5GB disk checking, ffprobe stream media component probing, and non-blocking REST notifications containing metadata sent to `VIDEO_SENDER_API_URL`. Implemented worker task retry loops with exponential backoff.
- [x] **Startup Integrity Cleanups:** Added database cleanup hooks in `main.py` on startup to reset any dangling tasks in `DOWNLOADING`, `MERGING`, or `MOVING_CLOUD` to `FAILED` with an "Interrupted by server shutdown/restart" error log.
- [x] **Transcode File Caching:** Created segment segment/file caching in `routes/stream.py` under `server/temp/transcode_cache/`. Cached dynamic transcode output is served directly via `FileResponse` for range-seek support.
- [x] **Admin TUI Upgrades (cli.py):** Added setting options to view, set, or clear `VIDEO_SENDER_API_URL` and implemented a fully interactive task scrolling list and detailed inspector pane in the Monitor Queue panel.
- [x] **Complete SVOD Tag Removal:** Removed all occurrences of the term 'SVOD' (case-insensitive) from all user-facing components, metadata descriptions, comments, and logging frameworks across the entire repository.
- [x] **Dynamic Rclone Settings Toggle & Fallback Resilience:** Designed a persistent settings system utilizing `settings.json` (disabled by default) to toggle the storage engine dynamically between `LOCAL` and `CLOUD`. Integrated toggle switches and subpath input fields in Settings view with automatic save-on-blur. Implemented automated fallback handlers that redirect assets from temporary folders to local media catalog directories if the cloud upload pipeline crashes.
- [x] **CLI Storage Settings TUI & Account Management:** Refactored `cli.py` storage engine and remote path prompts into an arrow-selectable sub-menu displaying values dynamically. Renamed User Center to "Account Management", removed the "Unlock User" option, and skipped email prompts for password resets/2FA setup, automatically targeting the registered default admin account.
- [x] **Automated Setup Wizard & Dependency Auto-Installers:** Created `setup.bat` (Windows) and `setup.sh` (Linux/macOS) to install Python 3.11, Node.js 20, local portable **FFmpeg/FFprobe** binaries, and **Rclone**. Implemented an interactive CLI TUI wizard (`cli.py --setup`) featuring dynamic ASCII banner parsing, admin register/2FA setup, verified TMDB token checks against Movie ID `290250` via httpx, and storage setup.

## What's Left to Build
- [x] **2FA Security Hardening:** Implemented TOTP 2FA authentication using `pyotp` for web client log-in session gate. Added login rate limiting with lockout blocks (15-minute lockouts after 5 consecutive password or code verification failures), interactive 2FA setup QR Code card widget inside user settings panel, and full administrative user management/2FA toggles/account unlocking capabilities in `cli.py` TUI console.
- [ ] **Rclone Upload Progress Tracking:** Parse rclone progress output to display upload percentage, speed, and ETA in the web dashboard interface instead of running silent background tasks.
- [ ] **Automatic Subtitle Auto-Conversion (VTT to SRT):** Automatically convert incoming WebVTT (.vtt) subtitles into SubRip (.srt) format during ingestion using a lightweight parser to maximize player compatibility.

## Current Status
The backend server and frontend streaming UI are highly stable, featuring secure user-level log-in gates, TOTP 2FA setup, responsive layouts, cinematic transitions and animations, robust profile settings, database resilience, metadata discovery, structured logging, stream caching, and an interactive CLI TUI. The core requirements for the self-hosted media platform are fully complete.
